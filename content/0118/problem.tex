\diarytitle{0118}{1次変換 u=x+y, v=x-y による重積分の計算}

1次変換 $u = x + y,\ v = x - y$ を用いて、次の積分を計算せよ。導出過程を示すこと。
\[
I = \iint_{D} (x^{2} - y^{2})\, e^{x+y}\, dx\, dy, \qquad D = \{(x,y) \mid 0 \le x+y \le 1,\ 0 \le x-y \le 1\}
\]
