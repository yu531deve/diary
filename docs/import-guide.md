# import/ 取り込みガイド

`import/` に置かれた既存原稿（Yu が過去に作成した院試問題集）を Diary の正規形（`content/{id}/`）に変換するための手順書。**この文書自体は調査・文書化のみを目的とし、`content/` と `import/` の中身は変更しない**（issue #126）。

実際の取り込み作業（issue #57 の子issue、1 問または少数単位で行う想定）を行う人向けに、`import/` の実物を見て判明した具体的な変換方法を記す。一般的な LaTeX 変換の作法ではなく、**このリポジトリの `import/` に実際にあるファイルに即した**内容である。

## 1. import/ の現状（2026-08-08 時点の調査結果）

```
import/
├── README.md
└── 01_分野別類題/
    ├── 01_ベルヌーイ型微分方程式/
    │   ├── problems.tex
    │   ├── problems.pdf
    │   ├── answers.tex
    │   └── answers.pdf
    ├── 02_リカッチ方程式/
    ├── ...
    └── 38_線積分/
```

- **38 テーマ**、各テーマに `problems.tex`（問題）と `answers.tex`（解答）のペア、およびビルド済み `problems.pdf` / `answers.pdf`。合計 152 ファイル。
- 各 `problems.tex` は `\shomon` マクロで小問を列挙しており、テーマあたり 3 問（一部 5 問）。**全テーマ合計で問題数は 124 問**（`\shomon` の出現数と `\kaitou` の出現数が一致することを確認済み）。
- 内容は大学数学（常微分方程式・偏微分方程式・線形代数・微分積分の計算問題）で、**大学院入試（理工系）レベルの類題集**。高校数学ではない。
- 図（TikZ）は **一切使われていない**（`grep -l tikzpicture` の結果 0 件）。取り込みにおいて図の SVG 化は考慮不要。
- `problems.tex` / `answers.tex` はいずれも `\documentclass[a4paper,11pt]{article}` + `\usepackage{preamble}`（+ `answers.tex` のみ `\usepackage{shoakubox}`）という独自 preamble に依存しており、**この `preamble.sty` / `shoakubox.sty` 自体は `import/` にも `diary` リポジトリ内にも存在しない**（外部の `00_common/` を `TEXINPUTS` で参照する前提のファイル）。取り込み時はこれらのマクロを Diary 標準の書き方に手作業で置き換える必要がある（後述）。

### テーマ一覧と分野マッピング（案）

`fields.yaml`（理工系院試 8 分野）に基づく仮割り当て。**最終判断は Yu が PR 上で行う**。

| # | テーマ | 仮 major | 仮 minor | 想定難易度 | 変換の手間 |
|---|--------|----------|----------|------------|------------|
| 01 | ベルヌーイ型微分方程式 | 常微分方程式 | 一階微分方程式 | 3 | 低 |
| 02 | リカッチ方程式 | 常微分方程式 | 一階微分方程式 | 4 | 低 |
| 03 | 一次同次線形微分方程式 | 常微分方程式 | 一階微分方程式 | 2 | 低 |
| 04 | 一次同次線形偏微分方程式 | 偏微分方程式・積分変換 | 熱・波動・ラプラス方程式 | 4 | 低 |
| 05 | 変数分離形 | 常微分方程式 | 一階微分方程式 | 1 | 低 |
| 06 | 同次形 | 常微分方程式 | 一階微分方程式 | 2 | 低 |
| 07 | 一階線形非同次 | 常微分方程式 | 一階微分方程式 | 2 | 低 |
| 08 | 完全微分方程式 | 常微分方程式 | 一階微分方程式 | 3 | 低 |
| 09 | クレロー方程式・ラグランジュ方程式 | 常微分方程式 | 一階微分方程式 | 4 | 低 |
| 10 | 定数係数二階線形同次 | 常微分方程式 | 定数係数線形方程式 | 2 | 低 |
| 11 | 未定係数法 | 常微分方程式 | 定数係数線形方程式 | 2 | 低 |
| 12 | 定数変化法 | 常微分方程式 | 定数係数線形方程式 | 3 | 低 |
| 13 | オイラー方程式 | 常微分方程式 | 定数係数線形方程式 | 3 | 低 |
| 14 | 階数低下法 | 常微分方程式 | 定数係数線形方程式 | 3 | 低 |
| 15 | 連立線形微分方程式 | 常微分方程式 | 連立系・解の構造 | 4 | 低（`pmatrix` あり、amsmath で対応可） |
| 16 | ラプラス変換 | 偏微分方程式・積分変換 | ラプラス変換 | 3 | 低 |
| 17 | べき級数解法 | 常微分方程式 | 級数解法 | 4 | 低 |
| 18 | スツルムリウヴィル境界値問題 | 常微分方程式 | 級数解法 | 5 | 中（用語が高度、要 Yu 確認） |
| 19 | 一階線形非同次偏微分方程式（ラグランジュの補助方程式） | 偏微分方程式・積分変換 | 熱・波動・ラプラス方程式 | 4 | 低 |
| 20 | 波動方程式（ダランベール） | 偏微分方程式・積分変換 | 熱・波動・ラプラス方程式 | 3 | 低 |
| 21 | 熱伝導方程式 | 偏微分方程式・積分変換 | 熱・波動・ラプラス方程式 | 3 | 低 |
| 22 | ラプラス方程式 | 偏微分方程式・積分変換 | 熱・波動・ラプラス方程式 | 3 | 低 |
| 23 | 波動方程式（有限区間） | 偏微分方程式・積分変換 | フーリエ級数・フーリエ変換 | 4 | 低 |
| 24 | 二階線形偏微分方程式の分類 | 偏微分方程式・積分変換 | 熱・波動・ラプラス方程式 | 4 | 低 |
| 25 | 対角化・固有値固有ベクトル | 線形代数 | 固有値と対角化 | 3 | 中（`\bm` 使用、後述） |
| 26 | 逆行列・行列式・行列方程式 | 線形代数 | 行列式 | 2 | 低（`pmatrix`/`vmatrix` は amsmath で対応可） |
| 27 | ケーリーハミルトン | 線形代数 | 固有値と対角化 | 3 | 低 |
| 28 | 最小二乗法 | 線形代数 | 内積空間・二次形式 | 4 | 中（`\bm` 使用） |
| 29 | 部分積分・置換積分 | 微分積分学 | 一変数の微分積分 | 2 | 低（5 問） |
| 30 | log・三角・有理関数の積分 | 微分積分学 | 一変数の微分積分 | 3 | 低（5 問） |
| 31 | 微分計算（合成・商・積） | 微分積分学 | 一変数の微分積分 | 1 | 低（5 問） |
| 32 | 微分計算（逆三角関数） | 微分積分学 | 一変数の微分積分 | 2 | 低（5 問） |
| 33 | 微分計算（多変数連鎖律） | 微分積分学 | 多変数の微分積分 | 3 | 低（5 問） |
| 34 | 複素指数積分・sinc | 微分積分学 | 広義積分 | 4 | 低 |
| 35 | ガウス積分 | 微分積分学 | 広義積分 | 3 | 低 |
| 36 | ヤコビアンと変数変換 | 微分積分学 | 重積分 | 4 | 低 |
| 37 | 重積分 | 微分積分学 | 重積分 | 3 | 低 |
| 38 | 線積分 | ベクトル解析 | 線積分・面積分 | 3 | 中（`\bm` 使用） |

想定難易度は `docs/difficulty.md` の基準に沿った暫定案（大学院入試レベルの計算演習であるため多くが 2〜4 に分布）。**確定は Yu が行う**。

## 2. 1 問あたりの変換手順

1. **ID 採番**: `content/` の既存最大 4 桁 ID + 1（`/new-problem` コマンドで雛形生成可）。振り直し・欠番の詰め直しはしない。
2. **`content/{id}/` に配置**: `meta.yaml` / `problem.tex` / `solution.tex` の 3 ファイルを作成。
3. **本文のみ形式への書き換え**（詳細は 3 章の対応表）:
   - `\documentclass`〜`\begin{document}` の preamble 一式を削除。
   - `\kamokumei{...}` は削除（Diary 側は `\diarytitle{id}{title}` で見出しを出す。ページ全体の科目見出しは不要）。
   - `problem.tex` の先頭に `\diarytitle{{id}}{{title}}` を追加。
   - `\shijibun{...}`（導入文）はそのまま地の文として残す、または問題文冒頭に統合。
   - `\shomon{問N.}{...}` は `\begin{enumerate}\item ...\end{enumerate}` に展開する（1 テーマ＝複数問なので、1 テーマ 1 問として import するなら該当する 1 問だけ抜き出し、複数問を 1 記事にまとめるなら enumerate 化）。**1 テーマ = 複数の独立した入試問題の類題集であり、Diary の 1 問 1 記事の粒度とは異なる**ため、テーマ内の各小問を個別の `content/{id}/` に分割するか、テーマ単位でまとめて 1 問（小問集合）にするかは **Yu の判断が必要**。
   - `answers.tex` の `\daimon{...}`（解法のまとめ見出し）は解答冒頭の解説文として残せる。
   - `\begin{mondaiboxS}{問N}...\end{mondaiboxS}`（問題文の再掲ボックス）は Diary 側に同等の環境がないため削除し、`solution.tex` は地の文から始める（`problem.tex` に既に問題文があるため再掲は本来不要）。
   - `\kaitou` 見出しはそのまま `\textbf{}` 等の地の文にするか削除して解答本文をそのまま続ける。
   - `\clearpage` は複数問を 1 ファイルに収める際の区切りとしてのみ必要なら残すが、Diary の 1 問 1 ファイル構成では通常不要になる。
4. **`meta.yaml` 起票**: `field.major` / `field.minor` は 1 章の表を参考に仮付けし、`tags.yaml` にある値のみ使用（無ければ追加提案を PR コメントに書き、辞書追加は別 issue）。`difficulty` は `docs/difficulty.md` 基準で暫定値、`status: draft` 固定（`published` は Yu のみ）。
5. **`make lint` / `make pdf` / `make html` を確認**（Definition of Done の 4 点）。
6. **`import/` から取り込んだ元ファイル（テーマフォルダ全体、または処理済みの元テキスト部分）を削除**。1 テーマ内の一部の問題だけ取り込んだ場合、残りの問題を含む元ファイルをどう扱うか（フォルダごと削除するか、取り込んだ小問だけ除去するか）は Yu に確認する。

## 3. 頻出パターンの書き換え対応表

| import/ 側の記法 | 対応 | 備考 |
|---|---|---|
| `\documentclass[a4paper,11pt]{article}` 〜 `\usepackage{shoakubox}` | 削除 | ビルド側が `diary.sty`（`jlreq` + `lualatex`）を適用するため本文のみ書く |
| `\begin{document}` / `\end{document}` | 削除 | 同上 |
| `\kamokumei{...}` | 削除、または `problem.tex` 冒頭の `\diarytitle{id}{title}` に統合 | Diary は `\diarytitle` が見出しを兼ねる |
| `\shijibun{...}` | 地の文として残す | マクロ定義なしでも普通の段落として機能する内容なので `\shijibun{}` を外して中身だけ残す |
| `\shomon{問N.}{text}` + 続く `\[ ... \]` | `\begin{enumerate}\item text \[ ... \] \end{enumerate}` に展開、または該当小問のみ抜粋 | 1 テーマ=複数問という単位の違いに注意（要 Yu 判断） |
| `\daimon{...}` | 解答冒頭の説明文として残す（マクロ除去） | 見出しの強調が欲しければ `\textbf{}` に置換 |
| `\begin{mondaiboxS}{...}...\end{mondaiboxS}` | 削除（中身の問題文再掲は不要。図表や強調が必要なら `\textbf{}`/`\begin{quote}` 等 amsmath 標準機能で代替） | `shoakubox.sty`（`tcolorbox` 依存）は Diary に存在せず、`\usepackage` 自体が lint で禁止 |
| `\kaitou` | 削除、または `\textbf{解答}\par` 等プレーンな強調に置換 | マクロ定義がないため必ず除去 or 代替が必要 |
| `\clearpage` | 基本削除（1 問 1 ファイルなので改ページ制御は不要） | 複数小問を 1 ファイルにまとめる場合のみ検討 |
| `\bm{...}`（25, 28, 38 番テーマで使用） | `\mathbf{...}` に置換、または太字ベクトルが必須なら `styles/diary.sty` への `bm` パッケージ追加を別 issue で提案 | `diary.sty` は `bm` を読み込んでおらず、`content/` 側で `\usepackage` するのは lint で禁止（`lint/forbidden-commands.yaml`）。保護ファイル変更が必要なため、この issue の範囲では手を出さない |
| `\usepackage{preamble}` / `\usepackage{shoakubox}` | 完全に削除 | `usepackage` は禁止コマンド。仮に preamble.sty 相当の機能が必要でも `content/` 側からは呼べない |
| `TEXINPUTS=...` を使ったコンパイルコメント行（`% コンパイル: ...`） | 削除 | Diary のビルドは `make pdf` / `make html` に統一されており無関係 |
| `\begin{pmatrix}` / `\begin{vmatrix}` / `\begin{align*}` / `\begin{cases}` / `\begin{aligned}` | そのまま使用可 | `amsmath` は `diary.sty` が読み込み済み |
| `\frac`, `\int`, `\sum`, `\lambda` 等の基本記法 | そのまま使用可 | `amsmath` / `amssymb` の範囲内 |
| TikZ 図 | 該当なし（`import/` の 38 テーマに図は存在しない） | もし今後 TikZ を含む原稿が追加された場合は `diary.sty` が `tikz`（`calc, intersections`）を読み込み済みなのでそのまま使えるが、独自 tikzlibrary が必要なら要相談 |

`lint/forbidden-commands.yaml` で禁止されているのは `write18` / `input` / `include` / `usepackage` の 4 つ。`import/` の原稿では `\usepackage` のみが該当し、これは preamble 削除の過程で自然に取り除かれる。`write18` / `input` / `include` の使用は 38 テーマ中どこにも見られなかった。

## 4. 詰まりやすい箇所・Yu に確認すべき事項

1. **粒度の不一致**: `import/` は「1 テーマ = 3〜5 問の類題集」という構成だが、Diary は「1 問 = 1 `content/{id}/`」。テーマ単位で 1 記事（複数小問の enumerate）にするか、小問ごとに ID を分けるか方針が必要。後者の場合、124 問すべてに個別 ID を振ることになり、`content/` が一気に増える。
2. **`\bm`（太字ベクトル）**: 25 対角化・28 最小二乗法・38 線積分の 3 テーマで使用。`\mathbf` で代替するか、`styles/diary.sty` に `bm` パッケージを追加するか（保護ファイル変更、別 issue 要）の判断が必要。
3. **問題文の再掲（`mondaiboxS`）の要否**: `answers.tex` は問題文をボックスで再掲してから解答する構成。Diary の `/solutions/{id}/` ページでは `problem.tex` と並べて表示される設計かどうかに依存するため、再掲を残すか削るかは site 側の仕様（`site/CLAUDE.md`・`docs/requirements.md`）と合わせて Yu が判断する。
4. **難易度判定**: 大学院入試の計算問題であり、`docs/difficulty.md` の想定利用者（大学受験生・国公立二次〜最難関大二次）の基準とは前提とする層が異なる。1 章の表の値はあくまで暫定であり、**院試レベルの原稿を Diary にそのまま載せてよいかという「レベル適合性」自体の判断も含めて Yu が行う**（README.md にも同旨の注記あり）。
5. **18 スツルムリウヴィル境界値問題**: 大学受験生向けとしてはかなり高度な内容（固有値問題・直交関数系）。取り込み対象とするか除外するか要確認。
6. **タグ付け**: `tags.yaml` は現状 4 行と語彙が少なく、38 テーマの内容（ベルヌーイ型、リカッチ、オイラー方程式 等）に対応するタグがほぼ存在しない。取り込み時に新規タグが多数必要になる見込みで、辞書追加をまとめて 1 issue で相談するか、テーマごとに都度相談するかの運用方針を決める必要がある。
7. **PDF 資産の扱い**: `import/` の `*.pdf` はビルド成果物であり、`content/` 側では PDF は `make pdf` で生成される。取り込み時にこれらの PDF をそのまま使うことはできない（コピー元のライセンス表記が Diary のものと異なるため）。取り込み完了後は `.tex` 同様に削除する。

## 5. 完了条件（この issue #126 の範囲）

- 本ファイルが存在し、`import/` の実物に即した具体的な手順になっている
- 取り込み候補一覧（ファイル名・推定分野・想定難易度・変換の難易度）を含む（1 章参照）
- `make lint` が通る
- `content/` と `import/` は無変更
